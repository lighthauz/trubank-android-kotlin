configurations.all {
    resolutionStrategy {
        failOnVersionConflict()

        def set = [] as Set

        set << "androidx.annotation:annotation:1.+"
        set << "androidx.core:core:1.+"

        force set.toArray()
        forcedModules = set
    }
}